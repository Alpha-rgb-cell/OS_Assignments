How to run this Assignment-2 (Simple-shell)

to compile simple-shell.c file: gcc -o simple-shell simple-shell.c

to run that file: ./simple-shell

for now it support commands like:

ls
ls -R
ls -l
echo
ls /home
wc -l fib.c
wc -c fib.c
grep printf helloworld.c
./fib 40
./helloworld
sort fib.c
uniq file.txt
cat fib.c | wc -l
cat helloworld.c | grep printf | wc -l

for bonus:

./my_command &
./myscript.sh
